name: crypto_screener
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '^3.11.0'

dependencies:
  flutter:
    sdk: flutter
  # Di bawah ini adalah package yang kamu pakai (pastikan spasinya sejajar)
  cupertino_icons: ^1.0.6
  http: ^1.6.0
  flutter_inappwebview: ^6.0.0
  intl: ^0.19.0
  web_socket_channel: ^3.0.3
  supabase_flutter: ^2.12.2
  url_launcher: ^6.3.2
  flutter_markdown: ^0.7.7+1
  geolocator: ^14.0.2
  geocoding: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/topi_toga.png
    - assets/trendline_theory.png
